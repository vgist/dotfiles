for pop3

    mkdir -p ~/.mutt/mail/inbox/{cur,new,tmp}
    mkdir -p ~/.mutt/mail/mbox/{cur,new,tmp}
    mkdir -p ~/.mutt/mail/postponed/{cur,new,tmp}
    mkdir -p ~/.mutt/mail/sent/{cur,new,tmp}
    mkdir -p ~/.mutt/cache/bodies/
    mkdir -p ~/.mutt/cache/headers/
    mkdir -p ~/.mutt/tmp
